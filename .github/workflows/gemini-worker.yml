name: AI Junior Dev (Aider)
on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  aider-job:
    if: contains(github.event.issue.body, '/fix') || contains(github.event.comment.body, '/fix')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Aider
        run: |
          python -m pip install --upgrade pip
          pip install aider-chat

      - name: Run Aider
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          HOME: /home/runner
        run: |
          git config --global user.name "Gemini AI"
          git config --global user.email "ai@example.com"
          
          # Added --suggest-shell-commands (so it knows it can run things)
          # Added a "System Prompt" hint to tell it NOT to just chat
          aider --model gemini/gemini-2.5-flash \
                --message "Task: ${{ github.event.issue.title }}. ${{ github.event.issue.body }}. IMPORTANT: If you need to delete, move, or create files, just do it. Do not just suggest the command." \
                --yes-always \
                --auto-commits \
                --map-tokens 0 \
                --no-stream \
                --exit
          
          git push origin main
