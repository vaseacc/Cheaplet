(edit-listing.html and listanitem.html are HTML source files, which are not build artifacts, dependencies, or temporary files - they are source code that should be tracked in version control)

(No files in the change list match the criteria for ignoring, so output nothing)